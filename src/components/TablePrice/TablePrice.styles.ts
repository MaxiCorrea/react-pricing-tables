import { createUseStyles } from "react-jss";

export const useTablePriceStyles = createUseStyles({
  priceSimbol: {
    fontFamily:"Rubik",
    fontSize : "26px",
  },
  priceValue : {
    fontFamily:"Rubik",
    fontSize : "46px",
  }
});
